pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build étape'
            }
        }

        stage('Test') {
            steps {
                echo 'Connexion OK'
            }
        }
    }
}
